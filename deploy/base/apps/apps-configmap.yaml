apiVersion: v1
kind: ConfigMap
metadata:
  name: mate-app-config
data:
  # These are patched by the deploy script based on the actual cluster routes.
  KEYCLOAK_AUTHORITY: "__REPLACED_AT_DEPLOY__"
  FRONTEND_PUBLIC_URL: "__REPLACED_AT_DEPLOY__"
  BACKEND_PUBLIC_URL: "__REPLACED_AT_DEPLOY__"
