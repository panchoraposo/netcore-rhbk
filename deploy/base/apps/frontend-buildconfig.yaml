apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: mate-portal
  labels:
    app.kubernetes.io/name: mate-portal
spec:
  runPolicy: Serial
  source:
    type: Binary
    binary: {}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Containerfile
  output:
    to:
      kind: ImageStreamTag
      name: mate-portal:latest
